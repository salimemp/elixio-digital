-- Auth models for Elixio Digital
-- Adds: email verification, password reset, magic link, OAuth (Google/GitHub),
-- passkeys (WebAuthn), MFA (TOTP + backup codes), login attempt audit log.
-- See apps/api/prisma/schema.prisma for the full model definitions.

-- This migration is generated by `prisma migrate diff` from the canonical
-- schema. To regenerate: pnpm db:generate
--
-- Apply with: pnpm db:migrate (dev) or `prisma migrate deploy` (CI/prod).

-- CreateEnum
CREATE TYPE "UserRole" AS ENUM ('buyer', 'creator', 'admin');

-- CreateEnum
CREATE TYPE "AssetStatus" AS ENUM ('draft', 'published', 'archived', 'rejected');

-- CreateEnum
CREATE TYPE "MediaKind" AS ENUM ('image', 'video');

-- CreateEnum
CREATE TYPE "LicenseCode" AS ENUM ('personal', 'commercial', 'extended');

-- CreateEnum
CREATE TYPE "OrderStatus" AS ENUM ('pending', 'paid', 'refunded', 'failed');

-- CreateEnum
CREATE TYPE "PayoutStatus" AS ENUM ('scheduled', 'processing', 'paid', 'failed');

-- CreateEnum
CREATE TYPE "OAuthProvider" AS ENUM ('google', 'github');

-- CreateEnum
CREATE TYPE "MfaFactorType" AS ENUM ('totp', 'webauthn');
